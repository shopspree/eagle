<%= form_for(@api_v1_comment) do |f| %>
  <% if @api_v1_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_v1_comment.errors.count, "error") %> prohibited this api_v1_comment from being saved:</h2>

      <ul>
      <% @api_v1_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
